services:
  - type: web
    name: form-scanner
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.15.1
    plan: free
